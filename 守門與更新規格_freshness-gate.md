# 記憶體儀表板：新鮮度守門與 manual 更新規格

> 2026-09-02 建立。接手這個專案、或下次要補 manual 訊號前先讀這份。

## 一、這份規格為什麼存在

儀表板有 9 個訊號，其中 6 個是人工填的。人工填的東西會過期，過期的分數會被當成現況讀。
2026-07-31 曾經發生過：分數卡在 3.2 綠燈 40 個交易日，期間 Micron Q3、SK hynix Q2、
三星 Q2 全部開牌過，一個都沒進分數。當時補了一道過期告警。

**但那道守門到 2026-09-02 都還是半殘的**，四個洞：

| # | 洞 | 後果 |
|---|---|---|
| 1 | 過期偵測走手寫白名單，漏掉 6c／8b／8d／8e | 這四項永遠不會被喊過期 |
| 2 | 沒有 `updated` 的子項會 fallback 繼承全域 `_last_updated` | 假新鮮。8b/8d/8e 內容其實停在 5/27，卻被當成 7/31 |
| 3 | 新鮮度權重從 8/17 就在算，但前端從來沒有渲染 | 算對了看不到，等於沒做 |
| 4 | 告警與結論各說各話 | 紅色過期告警旁邊，分數照出綠燈＋「可加碼」。人只會看結論 |

洞 2 的代價是實際發生的：`8e` 寫著「CXMT 無明顯擴產跡象」，而 CXMT 的產能規劃早已翻倍到
每月 60 萬片以上。**分數把 3.0（green）記成事實，整整一個月沒有任何機制會質疑它。**

## 二、現在的守門長什麼樣

單一來源：`calc_score.build_freshness()`。所有帶 `score` 的葉節點自動展開，
不再有白名單（新增子項自動納管）。輸出五個桶：

- **fresh** — 30 天內更新
- **stale** — 30~60 天
- **frozen** — 逾 60 天
- **unstamped** — 沒有 `updated`。**不繼承、不猜、不算新的**
- **awaiting** — 宣告了 `next_due`、還沒到期（見下節）

可信度判定用的分母是 **actionable_weight ＝ 有效權重 − awaiting 權重**，
也就是「現在就可以更新的部分裡，有多少是新的」。拿等財報的權重去扣自己的分數，
等於因為 Micron 還沒開牌而說儀表板不可信，那是兩件事。

| reliability | 條件 | 效果 |
|---|---|---|
| `ok` | fresh 佔可更新權重 ≥ 70% | 無附註 |
| `degraded` | 40~70% | 黃字附註「可看方向但不宜當作進出依據」 |
| `unreliable` | < 40% | 紅字「分數不可用於決策」，並前綴進 `status_text` |
| `overdue` | 任一項過了 `next_due` 沒補 | 同上，且優先於其他級別 |

70%／40% 是**拍板值不是實測校準**。校準方式寫在程式註解：比對補更新前後的分數落差，
<0.5 分表示門檻可放寬、>1.5 分表示要收緊。2026-09-02 首次量到的落差是 **0.31 分**
（3.32 → 3.63），單一樣本，先不動門檻。

**停用中的訊號**（目前 s3）不進告警——那會天天吵——改成 `disabled_signals` 常駐狀態塊，
前端固定顯示天數與待辦。上一版把停用訊號從告警排除後，它的待辦就再也沒有地方會提起，
那叫「排除即遺忘」。停用天數讀 `disabled_since`，不讀 `updated`（否則今天去補一句 note
就把積欠洗掉了）。

## 三、`next_due`：把「還沒得更新」跟「沒去更新」分開

放在葉節點上，宣告下一份新資料何時出現。**三條硬規則，不准放寬**：

1. 只能指向**外部事件日**（財報、公告、開牌），不能是「我下次想更新的日子」；
2. 過了 `next_due` 還沒更新 → **直接 overdue 紅燈，沒有 30 天寬限**；
3. **卡在人**（等 Jason 裁決、等我決定）的項目**不准用** `next_due`，那種就是該一直喊。

規則 3 的現行例子是 `s1c`：它卡在兩個待裁決問題（要比哪一組品項、校準區間怎麼訂），
所以刻意不給 `next_due`，讓它繼續叫。

`next_due` 是這道守門唯一可能被拿來消音的地方。要濫用它很容易——寫個遠一點的日期就安靜了。
所以判定日檢查（見第五節）第一條就是查有沒有人這樣做。

## 四、每次補 manual 訊號的動作

1. `data/manual_inputs.json` 逐項處理。**沒有新資訊就不要動 `updated`**——
   動了戳記卻沒動內容，等於對守門說謊。
2. 有新資訊但拿不到一手數字時：分數可以不動，但要把「拿到的是什麼、缺的是什麼」寫進 note。
   例：s7 的庫存週數自 5/27 起從未取得原廠一手數字，這個缺口已連續三次確認仍在。
3. 資料源是季報／財報的項目，掛 `next_due` + `due_reason`，不要假裝更新。
4. `venv\Scripts\python.exe scripts\calc_score.py`
5. `venv\Scripts\python.exe scripts\test_freshness_gate.py` — 31 項故障注入，全綠才算守門還活著
6. `venv\Scripts\python.exe tools\verify_dashboard_render.py` — 開真的瀏覽器確認讀數有出現在畫面上
7. 資料同步到 `docs/data/`（`update_today.ps1` Step 4 會做）

### 分數紀律

- 給分依既有階梯（如 S2 的 QoQ 階梯），**跨階梯時取中間值並寫明理由**，不要挑對自己方便的那一邊。
- 搜尋摘要給的數字若「正好回答判準門檻」，一律當未驗證，要落到一手文件才用。
  2026-09-02 就捨棄過一個：「4Q26 合約價 8-13%」查無一手來源，改用 TrendForce 6/30
  實際上修的 PC DRAM 3-8%。
- **標明「不可自行決定」的項目不要自己改分數**（目前是 s9）。有建議寫 `proposed_score`，
  分數本體留原值，讓 Jason 拍板。

## 五、這道守門本身的驗收

已登記修正有效性台帳：`20260902-freshness-gate`，family＝`儀表板新鮮度守門`，
**判定日 2026-10-02**（Micron FQ4 開牌後隔天，那時 `next_due` 機制會被真實檢驗）。

否證條件（任一成立即 failed）：

1. 10/02 當天 6b/6c 尚未依 Micron FQ4 更新，而 `freshness.overdue_items` 沒有列出它們；
2. `manual_inputs.json` 存在任何帶 `score` 的子項，既無 `updated` 也沒被列進 `unstamped_items`；
3. `signals.json` 缺 `reliability` 欄位，或 `verify_dashboard_render.py` 的可信度渲染檢查 FAIL；
4. `test_freshness_gate.py` 有任一項 FAIL。

## 五之二、三層觀測（不評分，2026-09-02 Jason 裁決後建立）

**共同原則：不評分不代表不用看見。** 這三層全部要渲染到看板上，
`verify_dashboard_render.py` 有對應的檢查項——只存在 JSON 而不顯示，等於沒做。

### 1. 供給側擴產（`data/supply_expansion.json`）

補上 8/28 自己列的缺口（「13 條訊號沒有一條在盯供給側擴產」）。**這一層只做一件事：
把每筆擴產的「產出日期」釘在時間軸上。**

**★建立這層時查到的關鍵分辨，比表格本身更重要**：SK hynix 8/07 核准的 380 億美元，
Y2 第一座無塵室 2029-06、M17 2028-12，**2029 年前一顆晶片都出不來**。
2029 前能加的供給全部來自已經在蓋的 **M15X 與龍仁一期（無塵室 2027 年初）**。
所以「新廠核准」不是把週期終點往前拉的證據，該盯的是已在進行的那批——
而龍仁一期的產出時間（2027H2~2028）正好撞上共識的週期終點。

**不要被什麼騙**（寫在檔案的 `watch_rules.what_does_not`）：宣布金額變大不等於供給變多；
資本支出年增率會把同一筆錢重複計算三年。只看無塵室、設備搬入這種會轉成產能的實體進度。

### 2. DDR5／DDR4 比值（`ratio_watch`，s1c 停用後的累積層）

三組口徑同時累積，另附 DDR4 自身的 20 日方向。
**解除條件是「樣本跨越一次 DDR4 價格轉折」，不是累積天數**——
現有 320 天全部落在 DDR4 因停產而稀缺的同一種狀態裡，再多天也是同一個特例。

### 3. 現貨 vs 合約並排（`contract_watch.spot_vs_contract`，s3 停用期間的替代觀察）

兩邊變動率並排，**不相除**（相除需要門檻，門檻正是現在沒有的東西）。三種狀態：

- `同向` — 兩邊一起漲
- `現貨鈍化` — 兩邊都漲但現貨漲幅已低於合約（**2026-09-02 現況：現貨 +5.14% vs 合約 +14.29%**）
- `背離` — 現貨轉負、合約仍漲

現貨領先合約 1~2 個月，所以順序是「現貨鈍化 → 現貨轉負 → 合約跟跌」。

## 六、目前掛著的待辦

| 項目 | 狀態 |
|---|---|
| **s3／s1b 的 16.5% 權重** | 停用累積中。解除條件①期別恢復前進＝9/02 已達成；②比值累積 ≥60 交易日＝9/02 時 17 天。**複查日 2026-11-10（trigger `memory-dashboard-s3-recalibrate`）**。★判定紀律：若那 60 天全在同一種市況（一路漲、無回檔），那是樣本不夠，不是可以定門檻 |
| **s1c 觀測層** | 2026-09-02 起累積。等 DDR4 價格轉折，同上複查日一起看 |
| **供給側擴產層** | 觀測中。下一個實體進度檢查點＝**2027-01 龍仁一期無塵室**（trigger `memory-supply-yongin-p1-cleanroom`）；期間若三星或美光跟進宣布同級別新廠則提前 |
| **s7 一手庫存週數** | 連續三次確認拿不到。另有 DIGITIMES 2026-09-02 反向標題（庫存回升）內文在付費牆後，已掛進 Micron FQ4 開牌當天第一順位覆核（財報有存貨天數可驗） |
| **s9** | 維持 5.0。裁決結論：擴產證據落在 2029、不影響 2027 時間軸，**該記在供給側觀測層而不是時間軸這格**。`proposed_score 5.5` 已移除 |

### 2026-09-02 三項裁決的結果與代價

1. **s9 維持 5.0**，供給側另開觀測層。
2. **s1c 改觀測不評分**。**★代價已知並接受：s1 內部只剩 1a，總分由 3.63 掉到 3.44**——
   拿掉的是一個 5.0 中性佔位，剩下的 1a 正好在 1.0 極端值。
   **分數變綠不是因為市況變好，是因為少了一個沒有資訊的中間值。**
   看板已標「1/3 個子項在運作」，`disabled_signals` 也會列出 s1c 與它的 3% 權重。
3. **s3／s1b 維持停用累積**，過渡期用上面第 3 層的並排觀察頂著。

**★由此浮出、但這次刻意沒動的結構問題**：複合訊號的子項停用後，
現有做法是「內部重新正規化」（剩下的子項承擔父訊號全部權重），
而不是按剩餘比例縮減父訊號的權重。s1 現在就是單一均線訊號獨佔 15%。
這個慣例 s4/s6/s8 都一樣，要改就是全面改，屬於權重結構調整——**下次重新校準權重時一併處理，不要單獨改 s1**。
